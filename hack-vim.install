pre_install(){
	rm -rf /etc/skel/.vim
	rm -f /etc/skel/.vimrc
	rm -rf ${HOME}/.vim
	rm -f ${HOME}/.vimrc
}

pre_upgrade(){
	rm -rf /etc/skel/.vim
	rm -f /etc/skel/.vimrc
	rm -rf ${HOME}/.vim
	rm -f ${HOME}/.vimrc
}
